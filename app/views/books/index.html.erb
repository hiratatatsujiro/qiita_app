<div class="test_index">
  <div class="test_list">
    <div class="test_name">
      <% @books.each do |book| %>
        <p>名前：<%= book.name %>(<%= book.price %>)</p>  
      <% end %>
    </div>
  </div>
</div>